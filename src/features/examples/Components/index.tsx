import React from "react";

import TestModal from "./TestModal";

const ComponentsExample = React.memo(() => {
  return (
    <section className="app-section">
      <TestModal />
    </section>
  );
});

export default ComponentsExample;
